







    // Main screen wrapper.
    // p-4 adds padding, flex flex-col gap-4 stacks items vertically with gaps.
    <div className="p-4 min-h-screen flex flex-col gap-4">
      
      {isLoading ? (
        // Loader
        <div className="flex justify-center items-center h-40">
          <span className="text-gray-500">Loading...</span>
        </div>
      ) : (
        // Main content
        <>
          <Dashboard fund={data.fund} balance={data.balance} />
          
          {/* 'Add project' button */}
          <button 
            onClick={handleOpenModal}
            // Tailwind classes for nice blue button
            className="w-full bg-blue-500 text-white font-semibold py-3 px-4 rounded-xl active:bg-blue-600 transition-colors"
          >
            Add project
          </button>
          
          <TransactionList transactions={data.transactions} />
        </>
      )}

      {/* Modal window (renders only if isModalOpen === true) */}
      {isModalOpen && (
        <ProjectModal 
          onClose={handleCloseModal} 
          onSuccess={loadDashboard} 
        />
      )}
      
    </div>
  );
};

export default App;
